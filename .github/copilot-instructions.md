---
description: AI Instructions ---


## Общие рекомендации (Эти рекомендации важны при работе)

- Отвечай на русском языке.
- Не создавай новых файлов пока не убедишься, что отсутсвуют такие же по функционалу файлы, чтобы не создавать дубликатов и путаницу в проекте.
- Поддерживай чистоту и аккуратность в структуре проекта, делай код максимально продуктивным и удобным, чтобы проект был на современном уровне профессионального senior разработчика.
- При необходимости и полезности этого действия, можешь делать запрос на переименование и удаление файлов, если они лишние или полностью дублируют функции других файлов, но также можешь делать резервные копии уже созданных файлов. Также дай возможность перепроверить, внесенные тобой именения и запрашивай запуск бота для проверки.
- Твои ответы должны быть подробными и комплексными, старайся вместить как можно больше продуктивной и полезной работы в каждый ответ.
- При возникновении проблем с UTF-8 encoding и форматированием. Можешь использовать replace_string_in_file tool для доработки текста и кода при необходимости.
- Разобравшись с одной задачей, не спрашивая у меня разрешения, можешь переходить к решению других задач.
- Используй всю полноту квоты сообщения, отвечай и работай пока не убедишься, что в проекте отсутсвуют недочеты, недоработки и ошибки. На каждой стадии разработки проекта ты должен стремиться к финальному состоянию продукта и кода, к его идеалу.
- При внесении изменений, как-либо связанных с базами данных, незамедлительно проведи нужные действия с имеющимися файлами баз данных, для правильной работоспособности проекта. (В том числе миграции)
- При обнаружении файлов дубликатов с одинаковыми функциями, создай новую, улучшенную версию файла, объединив лучшее из обеих версий. Но не уменьшай функциональности без необходимости, сохраняя всё важное и нужное в этих файлов для проекта, то что уже используется в проекте.
- make sure they're up to date with the latest python-telegram-bot version.
- Прежде чем создавать новый файл, изучи все возможные импорты в проекте, где могли бы использоваться эти функции файла. И не создавай нового файла, пока не проверишь в каждой папке наличие похожего или дублирующего файла с похожим названием. Можешь проводить поиск по ключевым словам в названии файла или строк кода в самих файлах, чтобы облегчить поиск.
- Прежде чем работать с терминалом и вводить команды, например, вводить команду перезапуска бота для проверки изменений, каждый раз активируй виртуальную среду.
- Если изменения незначительные, не создавай нового файла, а улучши и доработай имеющийся.
- Если какой-то файл содержит импорты на файл, которого нет в проекте, не создавай этого файла, проверь наличие существующего файла с этой функциональностью и просто измени импорт.
- Документация должна быть написана на русском языке. Размещай документацию структурированно с использованием отдельной папки docs. Создавай файлы документации по каждой сессии и актуально изменяй существующую документацию, но не создавай обилие похожих файлов документации, лучше меньше этих файлов, но более высокого качества.
- После изменений правильно коммить и запушивай проект на GitHub. Не пиши латиницей в виде транскрипции. Пиши коммиты по-русски кириллицей и сопровождай кратким переводом на английском.

## Рекомендации по работе с PowerShell

- На PowerShell нужно использовать Select-String вместо grep.
- На PowerShell нужно использовать Select-Object -First (без head).

## 🚀 Лучшие практики создания стартеров и скриптов с управлением терминалом

Стартеры (starter scripts) — критически важная часть улучшения пользовательского опыта. Вот как делать их профессионально и качественно для ВСЕХ проектов:

### 1. 📋 Архитектура Starter скрипта

**Основная структура:**
```python
# 1. Конфиг класс с константами
class Config:
    PROJECT_ROOT = Path(__file__).parent
    PORTS = {"api": 8000, "bot": 9000}
    
# 2. Утилиты для вывода
def print_banner()      # Красивый заголовок
def print_info()        # Информационные сообщения
def print_success()     # Успешные сообщения
def print_error()       # Ошибки
def print_warning()     # Предупреждения

# 3. Проверки перед запуском
def check_env()         # Проверить .env файл
def check_dependencies() # Проверить зависимости
def check_ports()       # Проверить свободные порты

# 4. Менеджер процессов
class ProcessManager:
    def start_component() # Запустить компонент
    def read_output()     # Читать логи
    def shutdown()        # Корректное завершение
    
# 5. Аргументы командной строки
argparse с режимами запуска

# 6. Главная функция с основным циклом
```

### 2. 🎨 Цветные логи с префиксами компонентов

**Обязательная реализация:**
```python
class Color:
    GREEN = '\033[92m'      # Зелёный
    BLUE = '\033[94m'       # Синий
    YELLOW = '\033[93m'     # Жёлтый
    RED = '\033[91m'        # Красный
    CYAN = '\033[96m'       # Голубой
    END = '\033[0m'         # Сброс цвета

# Функции для вывода
def print_info(component: str, message: str):
    """Информационное сообщение с префиксом компонента"""
    if component == "bot":
        prefix = "🤖 BOT"
        color = Color.GREEN
    elif component == "api":
        prefix = "🌐 API"
        color = Color.BLUE
    # ... и т.д.
    print(f"{color}[{prefix}]{Color.END} {message}")

def print_success(message: str):
    """Успешное сообщение"""
    print(f"{Color.GREEN}✅ {message}{Color.END}")

def print_error(message: str):
    """Ошибка"""
    print(f"{Color.RED}❌ {message}{Color.END}")
```

**Преимущества:**
- ✅ Легко отличить логи разных компонентов
- ✅ Красивый и профессиональный вывод
- ✅ Понятные emoji префиксы
- ✅ Работает на Windows, macOS, Linux

### 3. ⚙️ Проверка зависимостей ДО запуска

**Что проверять:**
```python
def check_env():
    """Проверить файл .env"""
    env_file = Config.PROJECT_ROOT / ".env"
    if not env_file.exists():
        print_error(".env файл не найден!")
        print_info("info", "Создайте .env с переменной BOT_TOKEN")
        sys.exit(1)
    
    load_dotenv(env_file)
    bot_token = os.getenv("BOT_TOKEN")
    if not bot_token:
        print_error("BOT_TOKEN не установлен!")
        sys.exit(1)
    
    print_success(".env файл найден")

def check_dependencies():
    """Проверить pip пакеты"""
    try:
        import telegram
        print_success("python-telegram-bot установлен")
    except ImportError:
        print_error("python-telegram-bot не установлен!")
        print_info("info", "Выполните: pip install -r requirements.txt")
        sys.exit(1)

def check_ports():
    """Проверить свободные порты"""
    import socket
    for name, port in Config.PORTS.items():
        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        result = sock.connect_ex(('127.0.0.1', port))
        sock.close()
        if result == 0:
            print_error(f"Port {port} ({name}) уже занят!")
            sys.exit(1)
    
    print_success("Все порты свободны")
```

### 4. 🛑 Корректное завершение при Ctrl+C

**Критически важно для Windows:**
```python
class ProcessManager:
    def __init__(self):
        self.processes = {}
        
    def start_bot(self):
        """Запустить с правильной обработкой сигналов"""
        cmd = [sys.executable, "src/main.py"]
        process = subprocess.Popen(
            cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,
            text=True,
            bufsize=1,
            # На Windows нужно None, на Unix можно lambda: signal.signal(...)
            preexec_fn=None if sys.platform == 'win32' else lambda: signal.signal(signal.SIGINT, signal.SIG_IGN)
        )
        self.processes['bot'] = process
        return process
    
    def shutdown(self, signum=None, frame=None):
        """Корректное завершение всех процессов"""
        print_warning("Завершаю все процессы...")
        
        for name, process in list(self.processes.items()):
            if process:
                print_info("info", f"Завершаю {name}...")
                try:
                    if sys.platform == 'win32':
                        # Windows: terminate + kill с таймаутом
                        process.terminate()
                        try:
                            process.wait(timeout=3)
                        except subprocess.TimeoutExpired:
                            process.kill()
                    else:
                        # Unix: term + kill
                        process.terminate()
                        try:
                            process.wait(timeout=3)
                        except subprocess.TimeoutExpired:
                            process.kill()
                except Exception as e:
                    print_error(f"Ошибка завершения {name}: {e}")
        
        print_success("Все процессы завершены")
        sys.exit(0)

# В main():
signal.signal(signal.SIGINT, manager.shutdown)
signal.signal(signal.SIGTERM, manager.shutdown)
```

### 5. 📖 Справка по всем режимам запуска

**Обязательное наличие:**
```python
# Использование argparse
parser = argparse.ArgumentParser(
    description="Starter для проекта",
    formatter_class=argparse.RawDescriptionHelpFormatter,
    epilog="""
Примеры использования:
  python start.py                # Запуск всего
  python start.py --ngrok        # С HTTPS туннелем
  python start.py --bot-only     # Только бот
  python start.py --api-only     # Только API
    """
)

parser.add_argument(
    "--ngrok",
    action="store_true",
    help="Запустить ngrok HTTPS туннель"
)

parser.add_argument(
    "--bot-only",
    action="store_true",
    help="Запустить только Telegram бот"
)

# И т.д. для каждого режима

args = parser.parse_args()

# Когда пользователь запустит: python start.py --help
# Он увидит всё это красиво отформатированным!
```

### 6. 📊 Информационный отчёт о запуске

**Показывать пользователю:**
```python
print_info("info", "=" * 60)
print_success("Все компоненты запущены!")
print()

if run_bot:
    print_info("bot", "🤖 Telegram Bot работает")
    print_info("bot", "   Отправьте /start в боте для начала")

if run_api:
    print_info("api", f"🌐 Mini App доступно: HTTP://{Config.API_HOST}:{Config.API_PORT}")

if run_ngrok:
    print_info("ngrok", "🔗 HTTPS туннель активен")

print_info("info", "=" * 60)
print_warning("Нажмите Ctrl+C для остановки всех процессов")
```

### 7. 🔄 Чтение логов из подпроцессов в реальном времени

**Правильная реализация:**
```python
def read_output(self):
    """Читать вывод всех процессов"""
    for name, process in list(self.processes.items()):
        if process and not process.poll():  # Проверить, живой ли процесс
            try:
                # readline() с таймаутом не блокирует основной цикл
                line = process.stdout.readline()
                if line:
                    line = line.rstrip('\n\r')
                    # Прибавить префикс компонента
                    prefix = self._get_prefix(name)
                    print(f"{prefix} {line}")
            except:
                pass

# В основном цикле:
try:
    while True:
        manager.read_output()
        time.sleep(0.1)  # Не перегружать CPU
except KeyboardInterrupt:
    manager.shutdown()
```

### 8. ✅ Чек-лист для идеального starter скрипта

Перед тем как закончить с starter скриптом, проверить:

- [ ] **Конфиг класс** с константами (порты, пути, префиксы)
- [ ] **Функции вывода** с цветными префиксами для каждого компонента
- [ ] **Проверка .env** перед запуском (BOT_TOKEN и другие)
- [ ] **Проверка pip пакетов** (import try/except для каждого)
- [ ] **Проверка портов** (socket.connect_ex для свободных портов)
- [ ] **ProcessManager класс** с методами start/read/shutdown
- [ ] **Signal handlers** (SIGINT, SIGTERM) для Ctrl+C
- [ ] **Корректное завершение** на Windows и Unix
- [ ] **ArgumentParser** со всеми режимами запуска
- [ ] **Информационный отчёт** после запуска (что где работает)
- [ ] **Чтение логов** из подпроцессов в реальном времени
- [ ] **Документация** (--help, README, QUICK_START)
- [ ] **Тестирование** на Windows, macOS, Linux
- [ ] **Обработка ошибок** для каждого компонента
- [ ] **Таймауты** для ожидания запуска компонентов

**Применяй эти практики ко ВСЕМ starter скриптам для проектов, и они будут выглядеть профессионально и работать надёжно!**