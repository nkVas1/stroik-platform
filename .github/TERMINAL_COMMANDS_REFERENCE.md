# 📋 Quick Terminal Command Reference

**Версия:** 1.0.0  
**Последнее обновление:** 16 ноября 2025  
**Применяется к:** Всем проектам Python с подобной структурой

---

## 🖥️ PowerShell (Windows)

### Активация виртуальной среды

```powershell
# Активировать venv
.\.venv\Scripts\Activate.ps1

# Проверить активацию
Write-Host $env:VIRTUAL_ENV  # Должен показать путь к .venv

# Выход из venv
deactivate
```

### Установка зависимостей

```powershell
# После активации venv
pip install -r requirements.txt

# Или через python -m
python -m pip install -r requirements.txt

# Проверить установленные пакеты
pip list

# Обновить pip
python -m pip install --upgrade pip
```

### Запуск приложения

```powershell
# После активации venv

# Основной запуск (заменить start.py на нужный скрипт)
python start.py

# С аргументами
python start.py --ngrok
python start.py --bot-only

# В фоне (не рекомендуется в PowerShell, используй start.py)
# Лучше использовать отдельный PowerShell window или tmux-подобный tool
```

### Работа с файлами и папками

```powershell
# Показать содержимое папки
Get-ChildItem
dir  # Альтернатива

# Изменить директорию
Set-Location (конец до имени папки)  # CD работает как alias
cd ./docs

# Показать абсолютный путь
Get-Location

# Найти файл
Get-ChildItem -Recurse -Filter "*.py"

# Показать содержимое файла
Get-Content filename.txt
cat filename.txt  # Альтернатива
```

### Git операции

```powershell
# Проверить статус
git status

# Добавить файлы
git add .

# Создать коммит
git commit -m "Сообщение коммита"

# Запушить изменения
git push origin main

# Просмотр логов
git log --oneline -10
```

### Запуск Python команд

```powershell
# Выполнить Python код
python -c "import sys; print(sys.version)"

# Запустить скрипт
python my_script.py

# Запустить модуль как скрипт
python -m uvicorn src.api:app

# Интерактивный Python
python
# или
python -i my_script.py  # С загрузкой скрипта
```

### Переменные окружения

```powershell
# Показать переменную
$env:PATH

# Установить на сессию
$env:DATABASE_URL = "postgresql://..."

# Установить постоянно (требует перезагрузки)
[System.Environment]::SetEnvironmentVariable("KEY", "value", "User")

# Удалить переменную
Remove-Item env:MY_VAR
```

### Проверка процессов

```powershell
# Показать все процессы Python
Get-Process python

# Остановить процесс
Stop-Process -Name python

# Или по ID
Stop-Process -Id 1234

# Список прослушиваемых портов
netstat -ano | findstr :8000
# или
Get-NetTCPConnection -LocalPort 8000
```

### Уведомления и паузы

```powershell
# Пауза (нажать любую клавишу)
Read-Host "Нажмите Enter для продолжения"

# Очистить экран
Clear-Host
# или
cls

# Эхо текст
Write-Host "Hello World"
Write-Host "Error text" -ForegroundColor Red
```

---

## 🐚 Bash (macOS/Linux)

### Активация виртуальной среды

```bash
# Активировать venv
source .venv/bin/activate
# или
. .venv/bin/activate

# Проверить активацию
echo $VIRTUAL_ENV  # Должен показать путь

# Выход из venv
deactivate
```

### Установка зависимостей

```bash
# После активации venv
pip install -r requirements.txt

# Проверить установленные пакеты
pip list

# Обновить pip
python -m pip install --upgrade pip

# Установить конкретную версию
pip install package==1.2.3
```

### Запуск приложения

```bash
# После активации venv
python start.py

# С аргументами
python start.py --ngrok
python start.py --bot-only

# В фоне (нежелательно, используй start.py)
nohup python start.py &

# В новом процессе
(cd /path/to/project && python start.py &)

# В tmux (если установлен)
tmux new-window -n myapp "python start.py"
```

### Работа с файлами и папками

```bash
# Показать содержимое папки
ls
ls -la  # С деталями и скрытыми файлами

# Изменить директорию
cd ./docs
cd ..
cd ~  # Home folder

# Показать абсолютный путь
pwd

# Найти файл
find . -name "*.py"
find . -type f -name "config*"

# Показать содержимое файла
cat filename.txt
less filename.txt  # С прокруткой
head -n 20 filename.txt  # Первые 20 строк

# Копировать файл
cp source.txt destination.txt

# Переместить/переименовать
mv old_name.py new_name.py

# Удалить файл
rm filename.txt
```

### Git операции

```bash
# Проверить статус
git status

# Добавить файлы
git add .
git add src/  # Только папку

# Создать коммит
git commit -m "Сообщение коммита"

# Запушить изменения
git push origin main

# Просмотр логов
git log --oneline -10

# Создать ветку
git checkout -b feature/new-feature

# Переключиться на ветку
git checkout main

# Удалить ветку
git branch -d old-branch
```

### Запуск Python команд

```bash
# Выполнить Python код
python -c "import sys; print(sys.version)"
python3 -c "import sys; print(sys.version)"  # macOS

# Запустить скрипт
python my_script.py

# Запустить модуль
python -m uvicorn src.api:app

# Интерактивный Python
python
ipython  # Если установлен
```

### Переменные окружения

```bash
# Показать переменную
echo $PATH

# Установить на сессию
export DATABASE_URL="postgresql://..."

# Установить постоянно (добавить в ~/.bashrc или ~/.zshrc)
echo 'export MY_VAR="value"' >> ~/.bashrc

# Просмотреть все переменные
env | grep MY_

# Удалить переменную
unset MY_VAR
```

### Проверка процессов

```bash
# Показать все процессы Python
ps aux | grep python
pgrep -l python

# Остановить процесс
kill 1234  # По ID
killall python  # Все Python процессы

# Процессы на порту
lsof -i :8000
# или
netstat -tlnp | grep :8000
ss -tlnp | grep :8000  # Современный способ
```

### Уведомления и паузы

```bash
# Пауза
read -p "Нажмите Enter для продолжения..."

# Очистить экран
clear

# Эхо текст
echo "Hello World"
echo -e "Hello\nWorld"  # С escape-последовательностями

# Цвет в Bash
echo -e "\033[32mGreen text\033[0m"
echo -e "\033[31mRed text\033[0m"
```

---

## 🔗 Комбинированные команды (работают в обоих)

### Полезные паттерны

```bash
# Цепочка команд (работает в PowerShell и Bash)

# PowerShell: используй ;
python -m pip install package ; python -m pytest

# Bash: используй &&
python -m pip install package && python -m pytest

# Bash: или || для error handling
python my_script.py || echo "Script failed"
```

### Проверки перед запуском

```bash
# Проверить Python версию
python --version
python3 --version

# Проверить pip
pip --version

# Проверить установку модуля
python -c "import module_name; print('OK')"

# Проверить путь Python
which python  # Bash
where python  # PowerShell
```

### Работа с текстом

```bash
# Найти строку в файле (работает везде)
grep "search_term" filename.txt
grep -r "search_term" ./src  # Рекурсивно

# Заменить в файле (Bash)
sed -i 's/old/new/g' filename.txt

# Подсчитать строки (везде)
wc -l filename.txt

# Отсортировать
sort filename.txt

# Уникальные строки
uniq filename.txt
```

---

## ⚙️ Специфичные команды проекта

### Для Kvasik Assistant Bot

```bash
# Активировать venv (обязательно перед любой Python командой!)
# PowerShell:
.\.venv\Scripts\Activate.ps1
# Bash:
source .venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить весь проект
python start.py

# Запустить бота отдельно
python start.py --bot-only

# Запустить API отдельно
python start.py --api-only

# Запустить с ngrok HTTPS туннелем
python start.py --ngrok

# Получить справку
python start.py --help

# Управление БД
python manage_db.py
python generate_migration.py "migration_name"

# Запустить тесты
pytest tests/
pytest tests/ -v  # Verbose
pytest tests/ --cov=src  # С coverage

# Проверить синтаксис
python -m py_compile src/main.py

# Форматировать код (если установлен black)
black src/

# Lint проверка (если установлен pylint)
pylint src/
```

---

## 🚨 Решение частых проблем

### "command not found" или "не является внутренней командой"

```
Причины:
1. Команда не установлена
2. Вenv не активирован
3. PATH неправильный

Решение:
- Активируй venv
- Проверь pip list
- Установи пакет если нужно
```

### Порт уже занят

```bash
# Найти процесс на порту

# PowerShell:
netstat -ano | findstr :8000
Get-NetTCPConnection -LocalPort 8000

# Bash:
lsof -i :8000
ss -tlnp | grep :8000

# Остановить процесс
# PowerShell:
Stop-Process -Id <PID>

# Bash:
kill <PID>
```

### ImportError при запуске

```
Причины:
1. Модуль не установлен
2. venv не активирован
3. PYTHONPATH неправильный

Решение:
- Активируй venv
- Запусти: pip install -r requirements.txt
- Проверь импорты в коде
```

### Медленный запуск

```
Возможные причины:
- Много модулей на загрузку
- Первый запуск (создание .pyc)
- Проблема с диском или сетью

Решение:
- Очистить __pycache__: find . -type d -name __pycache__
- Удалить .pyc: find . -type f -name "*.pyc" -delete
- Перезагрузить систему
```

---

## 📚 Справочные ссылки

- **Python**: https://docs.python.org/3/
- **pip**: https://pip.pypa.io/
- **PowerShell**: https://learn.microsoft.com/en-us/powershell/
- **Bash**: https://www.gnu.org/software/bash/manual/

---

**Используй эту справку как быструю ссылку перед выполнением команд!**
