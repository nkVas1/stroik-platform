/**
 * /find — временно перенаправляет на /onboarding.
 *
 * Страница сохранена в структуре (не удалена) для:
 * - сохранения SEO-веса URL;
 * - возможности в будущем реализовать публичный каталог исполнителей.
 *
 * Когда логика «заказчик ищет исполнителя» будет реализована,
 * этот файл заменится на полноценную страницу каталога.
 */
import { redirect } from 'next/navigation';

export default function FindPage() {
  redirect('/onboarding');
}
